---
description: List all IRIS projects
allowed-tools: Task
---

Use the `iris` subagent to list all projects.

The agent should run `iris project list` and present the output. Mark the active project if any.
