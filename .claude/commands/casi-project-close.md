---
description: Close the active CASI project
allowed-tools: Task
---

Use the `casi` subagent to close the active project.

The agent should run `casi project close` and confirm. After this, subsequent `casi run` calls will require opening or creating a new project before running.
