---
description: List all CASI projects
allowed-tools: Task
---

Use the `casi` subagent to list all projects.

The agent should run `casi project list` and present the output. Mark the active project if any.
