---
description: Close the active IRIS project
allowed-tools: Task
---

Use the `iris` subagent to close the active project.

The agent should run `iris project close` and confirm. After this, subsequent `iris run` calls will require opening or creating a new project before running.
