version: '3.8'

services:
  iris:
    build: .
    ports:
      - "4001:4001"
    volumes:
      - iris-data:/app/projects
    environment:
      - IRIS_ROOT=/app
      - NODE_ENV=production
      - IRIS_DAEMON_PORT=4002
    restart: unless-stopped

volumes:
  iris-data:
    driver: local
