## Per-project IRIS configuration.
## Values left blank/default; override as the project needs. See
## `src/iris/projects/CLAUDE.md` and `docs/memory-restructure.md` §6 for layout.

[project]
name = ""
description = ""
agent_notes = ""

[behavior]
# Per-project overrides of global behavior dials.
autonomy = ""    # "", "low", "medium", "high"
pushback = ""    # "", "light", "balanced", "rigorous"

[memory]
# Per-project overrides of memory dials.
slice_budget_tokens = 0  # 0 = inherit global default

[ops]
# Project-scoped op defaults and overrides.

[datasets]
# Dataset-specific preferences (populated as datasets are imported).
